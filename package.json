{
    "name": "@socialism/monorepo",
    "private": true,
    "type": "module",
    "packageManager": "pnpm@10.33.0",
    "scripts": {
        "build": "tsdown -W",
        "dev": "tsdown -W --watch",
        "test": "pnpm -r --if-present test",
        "changeset": "changeset",
        "version-packages": "changeset version",
        "release": "changeset publish"
    },
    "devDependencies": {
        "@rollup/plugin-yaml": "^5.0.0",
        "@types/node": "^26.1.2",
        "tsdown": "^0.22.14",
        "typescript": "^7.0.2"
    }
}